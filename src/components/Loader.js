import React from 'react';
import './components.css';

/**
 * Component to show Loading data, or a spinner etc
 */
const Loader = () => {
  return <div className="loader">Loading data...</div>
};

export default Loader;